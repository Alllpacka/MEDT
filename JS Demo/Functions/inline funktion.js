// Array mit Zahlen

const list = [1,2,3];

// Anforderung: erstelle list mi den doppelten ursprünglichen werten

const doubledList = list.map(function (number) { return number * 2 });

console.log(list);

console.log(doubledList);